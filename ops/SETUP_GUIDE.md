# 📖 Пошаговая инструкция по запуску сборки Проверочного документа

## Шаг 1: Установка Python библиотеки OpenAI

Откройте терминал в корне проекта и выполните:

```bash
pip install openai
```

**Проверка установки:**
```bash
pip show openai
```

Должна появиться информация о пакете (версия 1.0+).

---

## Шаг 2: Настройка API ключа OpenAI

### Вариант А: Переменная окружения (рекомендуется)

**В Windows (PowerShell):**
```powershell
$env:OPENAI_API_KEY="sk-proj-ваш-ключ-здесь"
```

**В Windows (CMD):**
```cmd
set OPENAI_API_KEY=sk-proj-ваш-ключ-здесь
```

**В Linux/WSL:**
```bash
export OPENAI_API_KEY="sk-proj-ваш-ключ-здесь"
```

### Вариант Б: Файл .env (постоянное хранение)

Создайте файл `.env` в корне проекта:

```bash
echo "OPENAI_API_KEY=sk-proj-ваш-ключ-здесь" > .env
```

**Внимание:** Файл `.env` добавлен в `.gitignore` и не будет закоммичен в Git.

### Где взять API ключ?

1. Зайдите на https://platform.openai.com/api-keys
2. Нажмите "Create new secret key"
3. Скопируйте ключ (он показывается только один раз!)
4. Используйте в команде выше

---

## Шаг 3: Запуск сборки документа

### Способ 1: Через терминал (самый простой)

```bash
# Базовая сборка без AI-анализа
python3 ops/build_check_document.py

# С AI-анализом противоречий (требует API ключ)
python3 ops/build_check_document.py --ai-analysis

# Через shell-скрипт (автоматически с AI)
./ops/build_check_document.sh
```

### Способ 2: Через Windows Explorer (двойной клик)

**Для Windows:** Дважды кликните на файл:
```
.obsidian\scripts\build-check-document.bat
```

Откроется окно терминала с результатами сборки.

### Способ 3: Горячая клавиша в Obsidian

1. Откройте **Settings** → **Hotkeys**
2. Найдите "Shell commands: Execute"
3. Добавьте свою горячую клавишу (например, `Ctrl+Shift+B`)
4. Выполните команду через эту клавишу

---

## Шаг 4: Проверка результата

После запуска проверьте файл:
```
content/0. Управление/0.5. Проверочный документ.md
```

**Что должно быть:**
- ✅ Временная метка сборки (вверху документа)
- ✅ 9 разделов с содержимым
- ✅ Раздел 9 с AI-анализом (если запускали с `--ai-analysis`)
- ✅ Правильная структура заголовков:
  - H2 (##) для цифровых разделов (1-9)
  - H3 (###) для буквенных подразделов (A-Z, А-Я)
  - H4+ (####) для всех остальных

---

## Альтернативный способ: Установка Shell Commands Plugin

Если хотите кнопку в Obsidian:

1. **Settings** → **Community plugins** → **Browse**
2. Найдите **"Shell commands"**
3. Установите и активируйте
4. **Settings** → **Shell commands** → **New shell command**
5. Добавьте команду: `bash ops/build_check_document.sh`
6. Назначьте иконку и горячую клавишу

---

## Быстрая проверка: Всё ли работает?

```bash
# Проверка 1: Python установлен
python3 --version

# Проверка 2: OpenAI библиотека установлена
python3 -c "import openai; print(openai.__version__)"

# Проверка 3: API ключ установлен
echo $OPENAI_API_KEY  # Linux/WSL
echo %OPENAI_API_KEY% # Windows CMD
$env:OPENAI_API_KEY   # Windows PowerShell

# Проверка 4: Скрипт запускается
python3 ops/build_check_document.py --help
```

---

## Troubleshooting

### Ошибка: "python3: command not found"

**Решение:** Используйте `python` вместо `python3`:
```bash
python ops/build_check_document.py
```

### Ошибка: "No module named 'openai'"

**Решение:** Установите библиотеку:
```bash
pip install openai
```

### Ошибка: "OPENAI_API_KEY not found"

**Решение:**
1. Проверьте что ключ установлен (команды из Проверка 3)
2. Убедитесь что используете правильный терминал (где устанавливали ключ)
3. Попробуйте вариант с файлом `.env`

### Раздел 9 содержит "AI-анализ недоступен"

**Причина:** OpenAI библиотека не установлена или нет API ключа

**Решение:**
1. Установите `pip install openai`
2. Настройте `OPENAI_API_KEY`
3. Запустите с флагом `--ai-analysis`

---

## Примеры использования

### Пример 1: Первый запуск
```bash
# Установка зависимостей
pip install openai

# Настройка ключа
export OPENAI_API_KEY="sk-proj-..."

# Сборка с AI-анализом
python3 ops/build_check_document.py --ai-analysis
```

### Пример 2: Быстрая пересборка
```bash
# Если ключ уже настроен ранее
./ops/build_check_document.sh
```

### Пример 3: Сборка без AI (быстрее и бесплатно)
```bash
python3 ops/build_check_document.py
# Раздел 9 будет с шаблоном для ручного заполнения
```

---

💡 **Совет:** Сохраните API ключ в файл `.env` чтобы не вводить каждый раз.

🎯 **Рекомендация:** Запускайте сборку после существенных изменений в документации для проверки согласованности проекта.
