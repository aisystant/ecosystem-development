# Быстрая инструкция по запуску скриптов

> **💡 Для пользователей Windows PowerShell:** Используйте файл **`USAGE_WINDOWS.md`** с командами для Windows!
>
> Эта инструкция для Linux/WSL bash.

---

## 📍 ГДЕ запускать

**ВСЕГДА** из корневой директории проекта:

```bash
# ✅ ПРАВИЛЬНО
cd /mnt/c/ecosystem-development
python3 ops/classify_documents.py
python3 ops/save_manual_edits.py
python3 ops/validate_classifications.py
```

```bash
# ❌ НЕПРАВИЛЬНО
cd /mnt/c/ecosystem-development/ops
python3 classify_documents.py  # Не сработает!
```

---

## 🔄 Полный workflow

### 1️⃣ Первый запуск (AI классификация)

```bash
cd /mnt/c/ecosystem-development
python3 ops/classify_documents.py
```

**Результат:** Таблица в документе 0.6 заполнена желтыми (AI) предложениями

---

### 2️⃣ Ручная правка

1. Откройте `content/0. Управление/0.6. Структура этого хранилища.md` в Obsidian
2. Найдите документ, который хотите отредактировать
3. **ВАЖНО:** Уберите **ВСЕ** теги `<mark>...</mark>` в строке

**Пример:**

Было (AI-предложение):
```markdown
| 5 | test.md | folder | <mark>doc</mark> | <mark>manual</mark> | <mark>mixed</mark> | ...
```

Стало (ручная правка - все теги убраны):
```markdown
| 5 | test.md | folder | doc | manual | mixed | methodology | global-core | public |
```

4. Сохраните файл

---

### 3️⃣ Сохранение ручных правок (желтое → зеленое)

```bash
cd /mnt/c/ecosystem-development
python3 ops/save_manual_edits.py
```

**Результат:**
- Создан/обновлен файл `ops/manual_classifications.json`
- Отредактированные строки сохранены

---

### 4️⃣ Повторная классификация (проверка защиты)

```bash
cd /mnt/c/ecosystem-development
python3 ops/classify_documents.py
```

**Результат:**
- Новые документы получат желтые (AI) предложения
- Сохраненные правки станут ЗЕЛЕНЫМИ
- **AI НЕ ИЗМЕНИТ зеленые значения**

---

### 5️⃣ Проверка валидности

```bash
cd /mnt/c/ecosystem-development
python3 ops/validate_classifications.py
```

**Результат:** Проверка что все значения соответствуют допустимым из документа 0.7

---

## ⚠️ Частые ошибки

### Ошибка 1: "Документ не найден"

```bash
❌ Документ не найден: /путь/к/content/0. Управление/0.6...
```

**Причина:** Скрипт запущен не из корневой директории

**Решение:**
```bash
cd /mnt/c/ecosystem-development
python3 ops/save_manual_edits.py
```

---

### Ошибка 2: "Ручных правок: 0"

```bash
📊 Статистика:
  ✅ Ручных правок (зеленые): 0
  🤖 AI-предложений (желтые): 39
```

**Причина:** В таблице нет строк, где **ВСЕ** значения без `<mark>` тегов

**Решение:** Убедитесь, что в строке убраны **ВСЕ 6** тегов `<mark>`:
- Type
- Audience
- Edit Mode
- Layer
- Scope
- Security

---

### Ошибка 3: "Невалидное значение"

```bash
❌ Строка 5: невалидное значение 'document' для оси 'type'
```

**Причина:** Использовано значение, которого нет в документе 0.7

**Решение:** Используйте только допустимые значения:
- Type: `doc`, `data`, `code`, `model`, `policy`, `contract`, `metric`, `economy`
- Audience: `manual`, `mixed`, `machine`
- Edit Mode: `manual`, `mixed`, `machine`
- Layer: `philosophy`, `methodology`, `ontology`, `program`, `conops`, `requirements`, `architecture`, `service`, `data`, `analytics`, `economy`, `content`
- Scope: `global-core`, `local-edge`
- Security: `public`, `internal`, `restricted`

---

## 🎯 Проверка текущего состояния

### Сколько ручных правок сохранено?

```bash
cat ops/manual_classifications.json | python3 -m json.tool | grep -c '"type"'
```

### Какие документы защищены?

```bash
cat ops/manual_classifications.json | python3 -m json.tool
```

### Проверить валидность:

```bash
python3 ops/validate_classifications.py
```

---

## 💡 Полезные команды

### Сбросить все ручные правки (вернуть к AI-предложениям):

```bash
# ВНИМАНИЕ: Это удалит ВСЕ сохраненные правки!
rm ops/manual_classifications.json
python3 ops/classify_documents.py
```

### Резервная копия ручных правок:

```bash
cp ops/manual_classifications.json ops/manual_classifications.backup.json
```

### Восстановить из резервной копии:

```bash
cp ops/manual_classifications.backup.json ops/manual_classifications.json
```

---

## 📞 Нужна помощь?

Полная документация: `ops/README_CLASSIFICATION.md`
