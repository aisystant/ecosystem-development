# Операционные скрипты и автоматизация

Этот каталог содержит скрипты для автоматизации процессов работы с документацией проекта.

---

## Генерация автоматических отчётов (НОВОЕ)

### Описание

Скрипт `build_report.py` генерирует автоматические отчёты по хранилищу знаний согласно ТЗ из раздела 0.4.1.

### Доступные отчёты

| Отчёт | Команда | Описание |
|-------|---------|----------|
| Архитектурный слепок | `--report architecture-snapshot` | Полное описание всех содержательных идей хранилища |
| Содержательная полнота | `--report content-completeness` | Сопоставление методологии 3×3 с документами, проверка SoTA-методов |
| Технические проблемы | `--report technical-issues` | Дублирование папок/документов/текста, битые ссылки |
| Терминология | `--report terminology` | Терминологическая согласованность (требует AI) |
| Рекомендации | `--report recommendations` | Рекомендации по развитию (требует AI) |
| Карта связей | `--report links-map` | Карта связей между документами |
| Все отчёты | `--report all` | Генерация всех отчётов сразу |

### Использование

#### Ручной запуск из терминала

```bash
# Из корня проекта!

# Генерация одного отчёта
python3 .ops/build_report.py --report architecture-snapshot

# Генерация всех отчётов
python3 .ops/build_report.py --report all

# Предпросмотр (не сохраняет)
python3 .ops/build_report.py --report technical-issues --dry-run

# Через shell-скрипт (все отчёты)
./.ops/build_all_reports.sh
```

#### Запуск из Obsidian

1. Установите плагин **Shell commands**
2. Добавьте команду: `bash .ops/build_all_reports.sh`
3. Назначьте горячую клавишу (например, `Ctrl+Shift+R`)

### Выходные файлы

Отчёты сохраняются в `content/0. Управление/0.4. Автоматические отчёты ИИ/`:

- `Архитектурный слепок хранилища 0.4.md`
- `Содержательная полнота описания 0.4.md`
- `Противоречия и несогласованности 0.4.md`
- `Терминологическая согласованность 0.4.md`
- `Рекомендации по развитию 0.4.md`
- `Карта связей между документами 0.4.md`

### Что анализирует скрипт

**Архитектурный слепок:**
- Миссия и ценности экосистемы
- Персоны и роли
- Проблемы, гипотезы, цели
- Целевая система (Созидатель)
- Функционирование и процессы
- Структура ИИ-платформы
- Данные и сущности
- Эпистемический статус
- Экономика и токеномика
- Метрики и статистика

**Содержательная полнота:**
- Матрица 3×3 (уровни × роли)
- Применение SoTA-методов (JTBD, C4, ADR, OKR и др.)
- Ответы на интересы стейкхолдеров
- Приоритизированные пробелы

**Технические проблемы:**
- Дублирование названий папок
- Дублирование названий документов
- Дублирование текста между документами
- Битые wikilinks
- Отсутствующие метаданные (type, status)

---

## Автоматическая сборка Проверочного документа

### Описание

Скрипт `build_check_document.py` автоматически собирает **Проверочный документ** (0.5) на основе структуры из документа **0.91. Структура проверочного документа**.

Проверочный документ - это верхнеуровневая сводка всех важных идей, решений и архитектуры проекта, которая помогает:
- Проверить согласованность принципов и решений
- Обнаружить противоречия между документами
- Получить краткий обзор всего проекта

### Структура документа

Документ собирается по следующей структуре:

1. **Зачем, для кого, что мы делаем** - миссия и цели экосистемы
2. **Проблемы, гипотезы и цели** - ключевые проблемы и пути решения
3. **Функционирование экосистемы** - как работает система
4. **Структура ИИ-платформы** - архитектура платформы
5. **Список подсистем** - реестр всех подсистем
6. **Схема взаимодействия** - как подсистемы взаимодействуют
7. **Процесс разработки** - организация работы
8. **Классификация и теги** - система организации документов
9. **Заключение о противоречиях** - анализ несоответствий (заполняется вручную)

### Использование

#### Ручной запуск из терминала

```bash
# Базовая сборка (без AI-анализа)
python3 .ops/build_check_document.py

# С AI-анализом противоречий (требует OPENAI_API_KEY)
python3 .ops/build_check_document.py --ai-analysis

# Через shell-скрипт (запускает с AI-анализом)
./.ops/build_check_document.sh
```

#### Запуск из Obsidian

**Способ 1: Через горячую клавишу (рекомендуется)**

Установите плагин **Shell commands**:
1. Settings → Community plugins → Browse → "Shell commands"
2. Settings → Shell commands → New shell command
3. Добавьте команду: `bash .ops/build_check_document.sh`
4. Назначьте горячую клавишу (например, `Ctrl+Shift+B`)

**Способ 2: Через Windows Explorer**

Для Windows: двойной клик на файл `.obsidian/scripts/build-check-document.bat`

**Способ 3: Через терминал в проекте**

```bash
# Из корня проекта
./.ops/build_check_document.sh
```

**Почему нет кнопки в Obsidian?**

Commander plugin не поддерживает запуск shell-скриптов напрямую. Для кнопки нужен дополнительный плагин **Shell commands** (см. Способ 1).

#### Автоматический запуск

Документ автоматически обновляется через GitHub Actions:
- ✅ При изменении файлов в `content/**/*.md`
- ✅ Еженедельно (каждый понедельник в 9:00 UTC)
- ✅ При ручном запуске workflow через GitHub UI

### Workflow: check-document-build.yml

Настроен автоматический пайплайн:

```yaml
Триггеры:
  - push в main/feat/ci-and-ops при изменении *.md
  - schedule: каждый понедельник в 9:00 UTC
  - workflow_dispatch: ручной запуск

Действия:
  1. Запуск build_check_document.py
  2. Проверка изменений
  3. Коммит и push (если есть изменения)
  4. Создание summary
```

### Исходные документы

Скрипт извлекает информацию из следующих документов:

| Раздел | Исходные документы |
|--------|-------------------|
| 1. Зачем, для кого | `1.4. Концепция функционирования`, `Манифест` |
| 2. Проблемы, гипотезы | `1.1. Проблемы`, `1.2. Гипотезы`, `1.5. Цели и задачи` |
| 3. Функционирование | `1.4. Концепция функционирования` |
| 4. Структура ИИ | `1.4. Концепция`, `4.2. Операционная система` |
| 5. Список подсистем | `4.1. Список подсистем` |
| 6. Схема взаимодействия | `1.4. Концепция` (раздел МИМ) |
| 7. Процесс разработки | `0.2. Процессы работы с хранилищем` |
| 8. Классификация | `0.7. Классификация документов и теги` |

### Настройка маппинга

Маппинг разделов на исходные документы настраивается в `DOCUMENT_MAPPING` внутри скрипта:

```python
DOCUMENT_MAPPING = {
    "1": {
        "title": "1. Зачем, для кого...",
        "sources": ["путь/к/документу.md"],
        "extract_sections": ["# Заголовок раздела"]
    },
    ...
}
```

### Расширение функциональности

Для добавления новых разделов:

1. Обновите `0.91. Структура проверочного документа.md`
2. Добавьте маппинг в `DOCUMENT_MAPPING`
3. Запустите скрипт для проверки

### AI-анализ противоречий

Скрипт поддерживает автоматический анализ противоречий через OpenAI API:

```bash
# Запуск с AI-анализом
python3 .ops/build_check_document.py --ai-analysis
```

**Что анализирует AI:**
- ✅ Соответствие заявленных целей и реализуемых систем
- ✅ Согласованность описанных процессов между разделами
- ✅ Противоречия в классификации документов
- ✅ Нестыковки в экономической модели
- ✅ Другие логические несоответствия

**Требования:**
- Переменная окружения `OPENAI_API_KEY` с API ключом
- Модель: gpt-4o-mini (быстро и недорого)

**Настройка API ключа:**

```bash
# Linux/macOS
export OPENAI_API_KEY="your-key-here"

# Windows (PowerShell)
$env:OPENAI_API_KEY="your-key-here"

# Или создайте .env файл в корне проекта
echo "OPENAI_API_KEY=your-key-here" > .env
```

### Фильтрация контента

Скрипт автоматически исключает текст, помеченный как "Не для ИИ":

**Поддерживаемые маркеры:**
```markdown
<!-- Не для ИИ -->
Этот текст будет исключён из анализа
<!-- /Не для ИИ -->

--------
Не для чтения ИИ:
Весь текст ниже этой линии будет исключён

Not for AI:
Everything below will be excluded
```

Это позволяет добавлять в документы:
- Личные заметки
- Черновики и TODOs
- Технические пометки для людей
- Метаинформацию о документе

### Структура заголовков

Скрипт автоматически корректирует уровни заголовков:

- **Цифровые разделы** (1, 2, 3...) → Заголовки уровня 2 (##)
- **Буквенные разделы** (А, Б, В...) → Заголовки уровня 3 (###)
- **Остальные заголовки** → Понижаются на один уровень

Это обеспечивает правильную иерархию в собранном документе.

### Проверка противоречий

После автоматической сборки рекомендуется:

1. Прочитать собранный документ
2. Проверить разделы на согласованность
3. Изучить раздел 9 "Заключение о противоречиях" (если запускали с --ai-analysis)
4. При обнаружении несоответствий - исправить исходные документы
5. Пересобрать документ для проверки

### Метрики

После сборки документ содержит:
- ✅ Временную метку сборки
- ✅ Ссылки на исходные документы
- ✅ Структурированные разделы
- ✅ Раздел для ручной проверки

### Troubleshooting

**Проблема**: Документ не собирается

**Решение**:
- Проверьте наличие исходных файлов
- Убедитесь что Python 3.11+ установлен
- Проверьте права доступа к файлам

**Проблема**: Пустые разделы

**Решение**:
- Проверьте что исходные документы содержат нужные заголовки
- Обновите `extract_sections` в маппинге
- Проверьте кодировку файлов (должна быть UTF-8)

### Roadmap

**Реализовано:**
- ✅ AI-анализ противоречий через OpenAI API
- ✅ Автоматическая фильтрация "Не для ИИ" контента
- ✅ Корректировка уровней заголовков
- ✅ Интеграция с Obsidian через Commander plugin
- ✅ GitHub Actions для автообновления

**В планах:**
- [ ] Интеграция с Obsidian Dataview для динамических запросов
- [ ] Генерация визуальных диаграмм связей между документами
- [ ] Экспорт в PDF/HTML форматы
- [ ] Детекция изменений в исходных документах (git diff)
- [ ] Поддержка других LLM провайдеров (Anthropic Claude, local models)
- [ ] Кэширование результатов AI-анализа
- [ ] Веб-интерфейс для просмотра противоречий

---

💡 **Совет**: Запускайте сборку после существенных изменений в документации для проверки согласованности проекта.
