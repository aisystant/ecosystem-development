# Система классификации документов

## Обзор

Автоматическая система классификации документов с защитой ручных правок.

## Цветовая маркировка

| Цвет | HTML | Значение | Можно изменить |
|------|------|----------|----------------|
| 🟨 Желтый | `<mark>value</mark>` | AI-предложение | AI может обновить при повторном запуске |
| 🟩 Зеленый | `<span style="background-color: lightgreen">value</span>` | Ручная правка человека | **ТОЛЬКО человек**, AI НИКОГДА не трогает |

## Скрипты

### 1. `classify_documents.py` - Автоматическая классификация

**Что делает:**
- Собирает все .md файлы в репозитории
- Использует GPT-4o-mini для классификации по 6 осям
- Создает желтые (AI) предложения для новых документов
- **НЕ ИЗМЕНЯЕТ** зеленые (ручные) значения

**Запуск:**
```bash
python .ops/classify_documents.py
```

**Важно:**
- Если документ есть в `.ops/manual_classifications.json` - AI вообще не запускается для него
- Ручные правки ЗАЩИЩЕНЫ от перезаписи

---

### 2. `save_manual_edits.py` - Сохранение ручных правок

**Что делает:**
- Читает таблицу из документа 0.6
- Находит значения БЕЗ тегов `<mark>` (то есть отредактированные вручную)
- Сохраняет их в `.ops/manual_classifications.json`
- При следующем запуске `classify_documents.py` они будут зелеными

**Запуск:**
```bash
python .ops/save_manual_edits.py
```

**Пример вывода:**
```
💾 Сохранение ручных правок из таблицы...

✅ Сохранена ручная правка: 0.0. Информация.md
✅ Сохранена ручная правка: 1.3. Идеи и принципы развития.md

📊 Статистика:
  ✅ Ручных правок (зеленые): 2
  🤖 AI-предложений (желтые): 37

💾 Ручные правки сохранены в: .ops/manual_classifications.json

🔒 Защита данных:
  • Зеленые значения НИКОГДА не будут изменены AI
  • Только человек может изменить зеленые значения
  • При запуске classify_documents.py зеленые останутся зелеными
```

---

### 3. `validate_classifications.py` - Проверка валидности

**Что делает:**
- Проверяет, что все значения соответствуют допустимым из документа 0.7
- Работает как с желтыми, так и с зелеными тегами
- Выдает понятные сообщения об ошибках

**Запуск:**
```bash
python .ops/validate_classifications.py
```

**Пример вывода при ошибке:**
```
❌ Строка 5 (test.md): невалидное значение 'document' для оси 'type'.
   Допустимые значения: doc, data, code, model, policy, contract, metric, economy
```

---

## Workflow использования

### Сценарий 1: Первая классификация

```bash
# 1. Запускаем классификацию
python .ops/classify_documents.py

# Результат: все документы с желтыми (AI) предложениями

# 2. Открываем документ 0.6 в Obsidian
# 3. Проверяем и редактируем значения вручную
# 4. Убираем теги <mark>...</mark> для значений, которые исправили

# 5. Сохраняем ручные правки
python .ops/save_manual_edits.py

# Результат: отредактированные значения теперь зеленые и защищены

# 6. Проверяем валидность
python .ops/validate_classifications.py
```

### Сценарий 2: Добавился новый документ

```bash
# 1. Запускаем классификацию
python .ops/classify_documents.py

# Результат:
# - Новый документ получил желтые (AI) предложения
# - Старые зеленые (ручные) значения НЕ ИЗМЕНИЛИСЬ

# 2. Проверяем новый документ в таблице
# 3. Если нужно - правим и сохраняем через save_manual_edits.py
```

### Сценарий 3: Нужно изменить зеленую правку

```bash
# 1. Открываем документ 0.6
# 2. Находим строку с зеленым значением
# 3. Меняем:
#    Было: <span style="background-color: lightgreen">doc</span>
#    Стало: <span style="background-color: lightgreen">model</span>

# 4. Проверяем валидность
python .ops/validate_classifications.py

# 5. Коммитим изменения
git add "content/0. Управление/0.6. Структура этого хранилища.md"
git commit -m "docs: обновлена классификация документа X"
```

---

## Защита ручных правок

### Как это работает

1. **Файл `.ops/manual_classifications.json`** хранит ручные правки:
```json
{
  "0. Управление/0.0. Информация.md": {
    "type": "doc",
    "audience": "manual",
    "edit_mode": "manual",
    "layer": "methodology",
    "scope": "global-core",
    "security": "public"
  }
}
```

2. **Скрипт `classify_documents.py` проверяет** наличие правки ПЕРВЫМ делом:
```python
if doc_path in manual_edits:
    # ✅ РУЧНАЯ ПРАВКА - используем ее БЕЗ изменений
    classification = manual_edits[doc_path]
    is_manual = True
else:
    # 🤖 AI-классификация (только если НЕТ ручных правок)
    classification = classify_document_with_ai(doc_path, content)
    is_manual = False
```

3. **Форматирование зависит** от источника:
```python
def format_value(value):
    if is_manual:
        # ✅ ЗЕЛЕНЫЙ = подтверждено человеком, AI НЕ ТРОГАЕТ
        return f'<span style="background-color: lightgreen">{value}</span>'
    else:
        # 🤖 ЖЕЛТЫЙ = предложение AI, человек может изменить
        return f'<mark>{value}</mark>'
```

### Гарантии защиты

- ✅ Если значение в `manual_classifications.json` - AI **НЕ ЗАПУСКАЕТСЯ** для этого документа
- ✅ Зеленые значения **НИКОГДА** не перезаписываются автоматически
- ✅ Только человек может изменить зеленые значения
- ✅ GitHub Actions проверяет валидность (но не меняет значения)

---

## Допустимые значения

Из документа `0.7. Классификация документов и теги.md`:

| Ось | Значения |
|-----|----------|
| **Type** | doc, data, code, model, policy, contract, metric, economy |
| **Audience** | manual, mixed, machine |
| **Edit Mode** | manual, mixed, machine |
| **Layer** | philosophy, methodology, ontology, program, conops, requirements, architecture, service, data, analytics, economy, content |
| **Scope** | global-core, local-edge |
| **Security** | public, internal, restricted |

⚠️ **ВАЖНО:** Можно использовать ТОЛЬКО эти значения! Валидация заблокирует коммиты с другими значениями.

---

## GitHub Actions

### Автоматическая валидация

Workflow `.github/workflows/validate-classifications.yml` запускается:
- При push в main или feature ветки
- При изменении файлов:
  - `content/0. Управление/0.6. Структура этого хранилища.md`
  - `content/0. Управление/0.7. Классификация документов и теги.md`
  - `.ops/validate_classifications.py`

**Что проверяется:**
- Все значения соответствуют допустимым из 0.7
- Работает с желтыми и зелеными тегами
- Выводит понятные сообщения об ошибках

**Exit codes:**
- `0` - все валидно, можно мерджить
- `1` - есть ошибки, нужно исправить

---

## FAQ

### Q: Как изменить желтое значение на зеленое?

A: Два способа:

**Способ 1** (рекомендуемый):
1. Открыть 0.6 в Obsidian
2. Убрать теги `<mark>...</mark>`, оставить только значение
3. Запустить `python .ops/save_manual_edits.py`

**Способ 2** (прямое редактирование):
1. Открыть 0.6 в редакторе
2. Заменить `<mark>value</mark>` на `<span style="background-color: lightgreen">value</span>`
3. Запустить `python .ops/save_manual_edits.py` для сохранения в JSON

---

### Q: Можно ли массово изменить все желтые на зеленые?

A: Да, но не рекомендуется без проверки:

```bash
# Сначала проверьте все AI-предложения вручную
# Затем запустите:
python .ops/save_manual_edits.py
```

Это сохранит ВСЕ текущие значения (желтые и зеленые) в JSON.

---

### Q: Что делать, если AI предложил неправильное значение?

A:
1. Открыть документ 0.6
2. Найти строку с документом
3. Изменить значение на правильное (из списка допустимых)
4. Убрать `<mark>` теги
5. Запустить `python .ops/save_manual_edits.py`
6. Проверить: `python .ops/validate_classifications.py`

---

### Q: Можно ли использовать свои значения, не из списка?

A: **НЕТ!** Валидация заблокирует коммит. Можно использовать только значения из документа 0.7.

Если нужно добавить новое значение:
1. Открыть документ 0.7
2. Добавить новое значение в соответствующую ось
3. Обновить `CLASSIFICATION_AXES` в скриптах
4. Использовать новое значение

---

### Q: Что произойдет, если удалить `manual_classifications.json`?

A: При следующем запуске `classify_documents.py`:
- Все значения станут желтыми (AI-предложения)
- Ручные правки будут потеряны
- Придется редактировать заново

**Рекомендация:** Храните `manual_classifications.json` в Git!

---

## Структура файлов

```
.ops/
├── classify_documents.py         # Автоматическая классификация
├── save_manual_edits.py          # Сохранение ручных правок
├── validate_classifications.py   # Проверка валидности
├── manual_classifications.json   # Хранилище ручных правок (создается автоматически)
└── README_CLASSIFICATION.md      # Эта документация

content/
└── 0. Управление/
    ├── 0.6. Структура этого хранилища.md  # Таблица классификации
    └── 0.7. Классификация документов и теги.md  # Определение осей

.github/workflows/
└── validate-classifications.yml  # GitHub Actions валидация
```

---

## Примеры

### Пример 1: Таблица с разными типами значений

```markdown
| № | Документ | Type | Audience |
|---|----------|------|----------|
| 1 | test.md | <mark>doc</mark> | <span style="background-color: lightgreen">manual</span> |
```

- **Type** = желтый (AI-предложение), можно править
- **Audience** = зеленый (ручная правка), защищено от AI

### Пример 2: Содержимое `manual_classifications.json`

```json
{
  "0. Управление/0.0. Информация.md": {
    "type": "doc",
    "audience": "manual",
    "edit_mode": "manual",
    "layer": "methodology",
    "scope": "global-core",
    "security": "public"
  },
  "1. Идеи развития экосистемы/1.3. Идеи и принципы развития.md": {
    "type": "doc",
    "audience": "manual",
    "edit_mode": "manual",
    "layer": "philosophy",
    "scope": "global-core",
    "security": "public"
  }
}
```

---

## Поддержка

При проблемах:
1. Проверить валидность: `python .ops/validate_classifications.py`
2. Проверить формат JSON: `python -m json.tool .ops/manual_classifications.json`
3. Создать issue на GitHub с описанием проблемы
