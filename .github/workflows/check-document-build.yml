name: Build Check Document

on:
  # Ручной запуск с возможностью выбора AI-анализа
  workflow_dispatch:
    inputs:
      ai_analysis:
        description: 'Запустить AI-анализ противоречий'
        required: false
        type: boolean
        default: true

  # Еженедельный запуск с AI-анализом (каждый понедельник в 9:00 UTC)
  schedule:
    - cron: '0 9 * * 1'

jobs:
  build-check-document:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build Check Document
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # Определяем нужен ли AI-анализ
          # - Всегда для schedule
          # - По выбору для workflow_dispatch
          # - Опционально для push (если есть API ключ)
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "📅 Еженедельный запуск - AI-анализ включен"
            python3 .ops/build_check_document.py --ai-analysis
          elif [ "${{ github.event_name }}" == "workflow_dispatch" ] && [ "${{ github.event.inputs.ai_analysis }}" == "true" ]; then
            echo "🔧 Ручной запуск с AI-анализом"
            python3 .ops/build_check_document.py --ai-analysis
          elif [ -n "$OPENAI_API_KEY" ]; then
            echo "🔑 API ключ найден - AI-анализ включен"
            python3 .ops/build_check_document.py --ai-analysis
          else
            echo "ℹ️ Базовая сборка без AI-анализа"
            python3 .ops/build_check_document.py
          fi

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet content/0.\ Управление/0.5.\ Проверочный\ документ.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add "content/0. Управление/0.5. Проверочный документ.md"
          git commit -m "chore: auto-update check document

          🤖 Автоматически обновлен Проверочный документ (0.5)

          Generated with [Claude Code](https://claude.com/claude-code)"

      - name: Push changes
        if: steps.check_changes.outputs.changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

      - name: Create summary
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "### 📊 Результаты сборки Проверочного документа" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Статус изменений
          if [ "${{ steps.check_changes.outputs.changed }}" == "true" ]; then
            echo "✅ Документ обновлен и закоммичен" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ Изменений не обнаружено" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY

          # AI-анализ статус
          if [ "${{ github.event_name }}" == "schedule" ] || [ "${{ github.event.inputs.ai_analysis }}" == "true" ] || [ -n "$OPENAI_API_KEY" ]; then
            echo "🤖 **AI-анализ:** включен" >> $GITHUB_STEP_SUMMARY
          else
            echo "📝 **AI-анализ:** отключен" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📍 **Файл:** \`content/0. Управление/0.5. Проверочный документ.md\`" >> $GITHUB_STEP_SUMMARY
          echo "⏰ **Время сборки:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "🔄 **Триггер:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
