# Настройка GitHub Secrets для CI/CD

Этот документ описывает как настроить секреты для автоматического запуска workflow с AI-анализом.

## OPENAI_API_KEY

### Зачем нужен

OPENAI_API_KEY используется для:
- **AI-анализа противоречий** в проверочном документе (0.5)
- **Автоматической классификации** новых документов
- **Еженедельных проверок** согласованности (каждый понедельник в 9:00 UTC)

Без API ключа workflow будет запускаться, но без AI-анализа (базовая сборка документа).

### Как получить ключ

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API Keys: https://platform.openai.com/api-keys
3. Нажмите "Create new secret key"
4. Скопируйте ключ (он будет показан только один раз!)
5. Сохраните ключ в безопасном месте

**Стоимость:** AI-анализ использует модель `gpt-4o-mini` (~$0.15 за 1M tokens), стоимость одного запуска ~$0.01-0.05.

### Как добавить в GitHub

**Для владельцев репозитория:**

1. Откройте настройки репозитория: `https://github.com/YOUR_ORG/ecosystem-development/settings`
2. В левом меню выберите **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Заполните:
   - **Name:** `OPENAI_API_KEY`
   - **Secret:** вставьте ваш API ключ
5. Нажмите **Add secret**

**Для контрибьюторов:**

Если у вас нет доступа к настройкам репозитория, обратитесь к владельцу/администратору с просьбой добавить секрет.

### Проверка работы

После добавления секрета:

1. Запустите workflow вручную:
   - Actions → Build Check Document → Run workflow
   - Выберите ветку: `main`
   - Включите опцию "Запустить AI-анализ противоречий"
   - Нажмите "Run workflow"

2. Проверьте логи workflow:
   - Шаг "Build Check Document" должен показать: `🔑 API ключ найден - AI-анализ включен`
   - Шаг "Create summary" должен показать: `🤖 AI-анализ: включен`

3. Проверьте результат:
   - Откройте файл `content/0. Управление/0.5. Проверочный документ.md`
   - В разделе "9. Заключение о противоречиях" должен быть AI-анализ

### Безопасность

**Важно:**
- ✅ Секреты GitHub зашифрованы и не видны в логах
- ✅ Секреты доступны только в workflow самого репозитория
- ✅ Секреты не доступны в PR из форков (защита от утечки)
- ❌ Не коммитьте API ключи в репозиторий
- ❌ Не добавляйте ключи в переменные окружения в коде
- ❌ Не храните ключи в `.env` файлах в репозитории

### Ротация ключей

Рекомендуется менять API ключи:
- Каждые 90 дней (плановая ротация)
- При подозрении на компрометацию
- При смене владельца/администратора

**Процесс ротации:**
1. Создайте новый ключ на OpenAI
2. Обновите секрет в GitHub (Settings → Secrets → Edit)
3. Удалите старый ключ на OpenAI
4. Запустите тестовый workflow для проверки

## Другие секреты (будущие)

### ANTHROPIC_API_KEY (опционально)

Для использования Claude API вместо OpenAI:
- Получить: https://console.anthropic.com/
- Добавить в GitHub Secrets аналогично OPENAI_API_KEY
- Модифицировать скрипты для поддержки Claude

### DATABASE_URL (в планах)

Для подключения к базе данных Memory Bank:
- Формат: `postgresql://user:password@host:port/database`
- Используется для хранения метаданных документов
- Пока не реализовано

## Локальное тестирование

Для локального запуска с AI-анализом:

```bash
# Экспорт ключа в текущую сессию
export OPENAI_API_KEY="your-key-here"

# Запуск скрипта
python3 .ops/build_check_document.py --ai-analysis

# Или через .env файл (НЕ коммитьте .env!)
echo "OPENAI_API_KEY=your-key-here" > .env
python3 .ops/build_check_document.py --ai-analysis
```

**Не забудьте добавить `.env` в `.gitignore`!**

## Troubleshooting

### Workflow запускается, но AI-анализ не работает

**Проблема:** В логах `ℹ️ Базовая сборка без AI-анализа`

**Решение:**
1. Проверьте что секрет `OPENAI_API_KEY` добавлен в настройках репозитория
2. Проверьте правильность имени секрета (точно `OPENAI_API_KEY`)
3. Проверьте что workflow использует правильную ветку (main/feat/ci-and-ops)

### API ошибки в логах

**Проблема:** `Error: OpenAI API request failed`

**Решение:**
1. Проверьте валидность API ключа на OpenAI
2. Проверьте баланс аккаунта OpenAI (должен быть положительным)
3. Проверьте rate limits (не превышен ли лимит запросов)
4. Попробуйте сгенерировать новый ключ

### Секрет не виден контрибьюторам

**Это нормально:** Секреты видны только владельцам репозитория. Контрибьюторы могут только запускать workflow, но не видеть значения секретов.

## Контакты

Вопросы по настройке:
- GitHub Issues: [создать issue](https://github.com/YOUR_ORG/ecosystem-development/issues)
- Документация: [README.md](../README.md)

---

Последнее обновление: 2025-11-12
