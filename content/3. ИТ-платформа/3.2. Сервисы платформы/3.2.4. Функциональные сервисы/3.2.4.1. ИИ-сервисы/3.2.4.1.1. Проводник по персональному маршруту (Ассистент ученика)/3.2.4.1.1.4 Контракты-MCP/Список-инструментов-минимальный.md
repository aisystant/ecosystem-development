# Список MCP-инструментов

Инструменты для Проводника: реализованные и планируемые.

---

## Реализованные инструменты (✅)

### dt.get_time_invested_summary

**Статус:** ✅ Реализован

**Назначение:** Получить сводку по времени обучения за указанный период.

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `period_start` | string (date) | Нет | Начальная дата (YYYY-MM-DD), по умолчанию -30 дней |
| `period_end` | string (date) | Нет | Конечная дата, по умолчанию сегодня |

**Ответ:**
```json
{
  "total_hours": 5.2,
  "sessions_count": 12,
  "pomodoros_completed": 8,
  "days_with_activity": 4,
  "avg_daily_minutes": 26,
  "period": {
    "start": "2025-12-16",
    "end": "2025-12-22"
  }
}
```

**Пример вызова:**
```bash
curl -X POST https://digital-twin-mcp.aisystant.workers.dev/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "dt.get_time_invested_summary",
      "arguments": {"period_start": "2025-12-16", "period_end": "2025-12-22"}
    },
    "id": 1
  }'
```

---

## Планируемые инструменты для Release 1 (🔜)

### dt.get_week_state

**Статус:** 🔜 Планируется

**Назначение:** Получить состояние недели — дни с активностью, количество действий по типам.

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `demo_profile` | string | Нет | `empty`, `weak`, `good` (для демо) |

**Ответ:** см. `Контракт_состояние-недели.json`

---

### dt.get_slot_consistency

**Статус:** 🔜 Планируется

**Назначение:** Получить систематичность слота саморазвития — сколько дней из недели удержан слот.

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `demo_profile` | string | Нет | `empty`, `weak`, `good` (для демо) |

**Ответ:** см. `Контракт_систематичность-слота.json`

---

### dt.get_work_products

**Статус:** 🔜 Планируется

**Назначение:** Получить количество и список рабочих продуктов за период.

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `demo_profile` | string | Нет | `empty`, `weak`, `good` (для демо) |

**Ответ:** см. `Контракт_рабочие-продукты-недели.json`

---

## Инструменты для Release 2 (⏳)

| Инструмент | Назначение | Статус |
|------------|------------|--------|
| `dt.build_week_plan` | Собрать план недели по двум числам | ⏳ R2 |
| `dt.save_week_plan` | Сохранить выбранный план недели | ⏳ R2 |
| `dt.save_week_review` | Сохранить итог недели | ⏳ R2 |
| `dt.configure_slot` | Настроить слот саморазвития | ⏳ R2 |
| `dt.save_product` | Сохранить рабочий продукт | ⏳ R2 |

---

## Соглашения по именованию

- **Префикс:** `dt.` (digital twin)
- **Формат:** `dt.<action>_<entity>`
- **Примеры:** `dt.get_time_invested_summary`, `dt.get_week_state`

---

## Принципы контрактов

1. **Структурированные ответы** — JSON с предсказуемой схемой
2. **Версионирование** — версия контракта указана в каждом JSON-файле
3. **Обратная совместимость** — новые поля optional, старые не удаляются

---

## Ссылки

- OpenAPI: `https://digital-twin-mcp.aisystant.workers.dev/openapi.json`
- MCP-эндпоинт: `https://digital-twin-mcp.aisystant.workers.dev/mcp`

---

**Последнее обновление:** 2025-12-23
