# Состояния ошибок и пустых данных

## Типы ошибок

### 1. Сетевая ошибка (Network Error)

**Причина:** Нет подключения к интернету или MCP-сервер недоступен.

**UI:**
```
⚠️ Не удалось подключиться

Проверьте интернет-соединение и попробуйте снова.

[🔄 Попробовать снова]
```

**Действия:**
- Retry 2 раза с задержкой 1 секунда
- Показать ошибку после 3 неудачных попыток
- Логировать для мониторинга

### 2. Таймаут (Timeout)

**Причина:** MCP-сервер не ответил за 5 секунд.

**UI:**
```
⏱️ Сервер не отвечает

Попробуйте позже или обновите страницу.

[🔄 Попробовать снова]
```

**Действия:**
- Отменить запрос после 5 секунд
- Показать сообщение
- Логировать для мониторинга

### 3. Ошибка сервера (Server Error)

**Причина:** MCP-сервер вернул ошибку (500, 502, 503).

**UI:**
```
🔧 Временные технические проблемы

Мы уже работаем над этим. Попробуйте через несколько минут.

[🔄 Попробовать снова]
```

**Действия:**
- Показать дружелюбное сообщение
- Логировать с деталями ошибки
- Не показывать технические детали пользователю

### 4. Ошибка авторизации (Auth Error)

**Причина:** Невалидный или просроченный токен (Release 2).

**UI:**
```
🔐 Сессия истекла

Войдите снова, чтобы продолжить.

[🔑 Войти]
```

**Действия:**
- Очистить токен
- Перенаправить на авторизацию
- Сохранить контекст для возврата

## Пустые данные

### Пустая неделя (No Activity)

**Условие:** `days_with_activity === 0`

**UI:**
```
📅 На этой неделе пока нет активности

Каждый путь начинается с первого шага. Попробуйте начать
с 10 минут чтения — даже малое действие лучше ожидания.

[📖 Открыть курс]
```

**Тон:** Поддерживающий, не осуждающий.

### Частично пустые данные

| Метрика | Условие | Отображение |
|---------|---------|-------------|
| Время | `total_minutes === 0` | "Время: пока не зафиксировано" |
| Слот | `primary_slot === null` | Скрыть блок систематичности |
| Продукты | `total_count === 0` | "Рабочих продуктов: пока нет" |

## Обработка edge cases

### Первый день недели (понедельник)

```
📅 Неделя только началась

Пока мало данных для анализа. Загляните в конце недели
для полной картины.

[📖 Начать заниматься]
```

### Демо-режим

```
🎭 Демо-режим

Вы просматриваете примерные данные. Для реальной статистики
подключите свой аккаунт.

[🔑 Подключить аккаунт]  (Release 2)
```

## Логирование ошибок

Все ошибки логируются с полями:

| Поле | Описание |
|------|----------|
| `timestamp` | Время ошибки |
| `error_type` | Тип ошибки |
| `twin_id` | ID пользователя (если известен) |
| `tool_name` | Инструмент MCP |
| `error_message` | Сообщение ошибки |
| `retry_count` | Количество попыток |

---

**Связанные документы:**
- [[Карта-экранов-и-состояний]]
- [[../3.2.4.1.1.2 Архитектура/Потоки-данных-и-состояний]]
