---

type: doc
audience: mixed
edit_mode: manual
layer: service
scope: local-edge
security: internal
status: draft
version: 0.1
created: 2025-11-12
---


# 14. Система идентификации и доступа (ORY)

Централизованная система аутентификации и авторизации на основе ORY Ecosystem, обеспечивающая Single Sign-On (SSO), управление идентификациями, контроль доступа (ACL) и управление согласиями для всех сервисов экосистемы.

## Назначение

Основная цель системы и решаемые задачи:
- **Единая точка аутентификации** — SSO для всех сервисов экосистемы
- **Управление идентификациями** — создание, обновление, удаление профилей пользователей
- **Контроль доступа** — централизованное управление правами и разрешениями (ACL)
- **Управление согласиями** — GDPR-совместимое управление consent пользователей
- **Федеративная аутентификация** — поддержка OAuth2, OpenID Connect, SAML
- **Безопасность** — защита от атак, MFA (многофакторная аутентификация)

## Архитектура

### Компоненты

1. **ORY Kratos** (Identity Management)
   - Управление идентификациями пользователей
   - Self-service flows: регистрация, логин, восстановление пароля, верификация email
   - Управление профилями и атрибутами
   - MFA (TOTP, WebAuthn)

2. **ORY Hydra** (OAuth2/OpenID Connect Provider)
   - OAuth2 и OpenID Connect сервер
   - Выдача токенов доступа (access tokens, refresh tokens, ID tokens)
   - Управление клиентами (applications)
   - Consent management

3. **ORY Keto** (Access Control)
   - Управление разрешениями на основе отношений (Zanzibar-like)
   - ACL (Access Control Lists) и RBAC (Role-Based Access Control)
   - Проверка прав доступа к ресурсам
   - Делегирование прав

4. **ORY Oathkeeper** (Identity & Access Proxy)
   - Reverse proxy для защиты сервисов
   - Валидация токенов
   - Трансформация запросов (добавление user context в headers)
   - Rate limiting и защита от атак

### Технологический стек

- **Backend:** ORY Kratos, ORY Hydra, ORY Keto, ORY Oathkeeper (Go)
- **Frontend:** Self-service UI (React/Next.js) для регистрации и логина
- **База данных:** PostgreSQL (для всех ORY компонентов)
- **Инфраструктура:** Kubernetes, Helm charts, Nginx Ingress

## Интеграции

### Входящие интеграции

Все сервисы экосистемы используют эту систему для аутентификации и авторизации:
- [[4.2. Операционная система ИИ-платформы]] — авторизация вызовов ИИ-агентов
- [[4.3. Клуб]] — SSO для участников клуба
- [[4.4. Платформа обучения]] — аутентификация учащихся
- [[4.7. CRM]] — управление доступом к данным клиентов
- [[4.9. Биллинг]] — авторизация платежных операций
- [[3.4. Описание единого хранилища знаний (Memory Bank)]] — контроль доступа к знаниям
- [[4.15. Apps SDK и маркетплейс ассистентов]] — авторизация публикации ассистентов

### Исходящие интеграции

Система использует следующие внешние сервисы:
- [[4.11. Цифровой двойник]] — связь identity с профилем пользователя
- Внешние IdP (Identity Providers): Google, GitHub, Microsoft для федеративной аутентификации
- Email сервис для верификации и уведомлений

## API и контракты

### Основные эндпоинты

**ORY Kratos (Identity Management):**
```
POST /self-service/registration/browser       # Инициация регистрации
POST /self-service/login/browser             # Инициация логина
POST /self-service/recovery/browser          # Восстановление пароля
POST /self-service/verification/browser      # Верификация email
GET  /sessions/whoami                        # Текущая сессия
DELETE /sessions                             # Логаут
GET  /admin/identities/:id                   # Получение идентификации
PATCH /admin/identities/:id                  # Обновление идентификации
```

**ORY Hydra (OAuth2/OIDC):**
```
POST /oauth2/token                           # Получение access token
GET  /oauth2/auth                            # Authorization endpoint
POST /oauth2/revoke                          # Отзыв токена
GET  /userinfo                               # OIDC userinfo endpoint
GET  /.well-known/openid-configuration       # OIDC discovery
```

**ORY Keto (ACL):**
```
POST /relation-tuples                        # Создание отношения
DELETE /relation-tuples                      # Удаление отношения
POST /check                                  # Проверка прав доступа
GET  /expand                                 # Развёртывание прав
```

### События

Система генерирует следующие события:
- `identity.created` — создана новая идентификация
- `identity.updated` — обновлена идентификация
- `session.created` — создана сессия (логин)
- `session.terminated` — завершена сессия (логаут)
- `mfa.enabled` — включена MFA
- `consent.granted` — дано согласие на доступ
- `token.issued` — выдан токен доступа
- `token.revoked` — отозван токен

## Хранилище данных

### Схема данных

Основные сущности и их поля:
- **Identity** (Kratos): id, schema_id, traits (email, name, etc.), state, created_at, updated_at
- **Session** (Kratos): id, identity_id, active, expires_at, authenticated_at, authentication_methods
- **Credential** (Kratos): id, identity_id, type (password, oidc, webauthn), config, created_at
- **OAuth2Client** (Hydra): id, client_name, client_secret, redirect_uris, grant_types, scope
- **OAuth2Token** (Hydra): signature, request_id, client_id, subject, expires_at, scope, token_type
- **RelationTuple** (Keto): namespace, object, relation, subject_id (для ACL)
- **Consent** (Hydra): challenge, subject, client_id, granted_scope, granted_at, expires_at

### Retention policy

- **Активные сессии:** До истечения срока действия (обычно 24 часа)
- **Отозванные токены:** 30 дней
- **История изменений идентификаций:** 1 год
- **Логи аутентификации:** 90 дней (для аудита)
- **Consent записи:** До отзыва пользователем или 2 года

## Метрики и мониторинг

### Ключевые метрики

| Метрика | Целевое значение | Текущее |
|---------|------------------|---------|
| Availability (SSO) | 99.95% | — |
| Response time (p95) для login | <300ms | — |
| Response time (p95) для token validation | <50ms | — |
| Error rate (failed logins) | <5% | — |
| MFA adoption rate | >30% | — |
| Token refresh success rate | >99% | — |

### Алерты

**Критические:**
- Availability < 99.5% за последние 5 минут
- Response time (p95) > 1s за последние 5 минут
- Failed login rate > 20% (возможная атака)
- Database connection pool exhausted

**Предупреждения:**
- Response time (p95) > 500ms за последние 15 минут
- Failed login rate > 10%
- Token validation error rate > 1%
- Certificate expiration < 30 дней

## Roadmap

### v1.0 (MVP)
- [ ] Развёртывание ORY Kratos (identity management)
- [ ] Развёртывание ORY Hydra (OAuth2/OIDC provider)
- [ ] Self-service UI для регистрации и логина
- [ ] Базовый RBAC через ORY Keto
- [ ] Интеграция с Digital Twin (4.11)
- [ ] Документация API и интеграционные гайды

### v1.1
- [ ] MFA (TOTP, WebAuthn) для всех пользователей
- [ ] Федеративная аутентификация (Google, GitHub, Microsoft)
- [ ] Расширенная модель ACL в ORY Keto
- [ ] Развёртывание ORY Oathkeeper (API Gateway)
- [ ] Session management dashboard для администраторов
- [ ] Rate limiting и защита от brute-force

### v2.0
- [ ] Adaptive authentication (риск-основанная аутентификация)
- [ ] Passwordless authentication (WebAuthn only)
- [ ] Децентрализованная идентификация (DID/Verifiable Credentials)
- [ ] Advanced fraud detection (ML-based)
- [ ] Мультитенантность (изоляция данных по организациям)

## Связанные документы

- [[4.1. Список подсистем]] — реестр всех систем
- [[4.0. Информация]] — обзор раздела Системы
- [[0.7. Классификация документов и теги]] — Frontmatter

## История изменений

| Дата | Версия | Описание |
|------|--------|----------|
| 2025-11-12 | 0.1 | Первое создание документа |

---

**Status:** 🟡 Draft
**Owner:** Security & Infrastructure Team
**Last Updated:** 2025-11-12
