# Работа с Claude Code

Руководство по использованию Claude Code для разработки экосистемы.

## О Claude Code

**Claude Code** — это официальный CLI от Anthropic для работы с Claude AI. Инструмент позволяет взаимодействовать с кодовой базой, анализировать проекты и выполнять задачи разработки через командную строку или интерактивный режим.

- **Модель:** Claude 3.5 Sonnet (claude-sonnet-4-5-20250929)
- **Контекстное окно:** 200k токенов
- **Режимы работы:** CLI, интерактивный, интеграция с IDE

## Быстрый старт

### Установка

```bash
# Установка через npm
npm install -g @anthropic-ai/claude-code

# Или через pip
pip install claude-code

# Проверка установки
claude --version
```

### Настройка API ключа

```bash
# Установить API ключ Anthropic
export ANTHROPIC_API_KEY='your-api-key-here'

# Или через конфигурационный файл
claude config set api_key YOUR_API_KEY
```

### Первый запуск в проекте

```bash
# Перейти в директорию проекта
cd /path/to/ecosystem-development

# Запустить интерактивный режим
claude

# Или задать вопрос напрямую
claude ask "Какая структура репозитория?"
```

## Настройка для этого проекта

### Конфигурация

Настройки Claude Code для этого репозитория находятся в `.claude/settings.local.json`:

```json
{
  "context": {
    "directories": [
      "content/",
      "artifacts/",
      "agents-core/",
      "ops/"
    ],
    "exclude": [
      "node_modules/",
      ".git/",
      "*.zip",
      "repo_full*.zip"
    ]
  },
  "model": {
    "name": "claude-sonnet-4-5",
    "temperature": 0.7,
    "max_tokens": 8000
  }
}
```

### Важные контексты

Claude автоматически загружает контекст из:
- `content/0. Управление/` — стратегия и процессы проекта
- `content/agents/` — описания AI-агентов
- `.github/workflows/` — CI/CD конфигурация
- `ops/` — операционные скрипты

## Основные команды

### Работа с кодом

```bash
# Анализ файла
claude analyze ops/classify_documents.py

# Рефакторинг
claude refactor agents-core/docs/agent.py --improve-performance

# Добавление функционала
claude implement "Добавь валидацию входных параметров в classify_documents.py"

# Поиск багов
claude debug "Почему build_check_document.py падает на больших файлах?"
```

### Работа с документацией

```bash
# Создание документа
claude doc create "Опиши архитектуру системы 4.13"

# Улучшение существующего
claude doc improve content/4.\ Системы/4.7.\ CRM.md

# Проверка качества
claude doc check content/ --metrics readability,completeness

# Генерация README
claude doc generate-readme agents-core/analytics/
```

### Архитектурные вопросы

```bash
# Консультация по архитектуре
claude ask "Как лучше интегрировать систему аналитики с Memory Bank?"

# Сравнение вариантов
claude compare "Сравни PostgreSQL и ClickHouse для системы аналитики"

# Оценка технологий
claude evaluate "Оцени использование Kafka vs RabbitMQ для нашей нагрузки"
```

### Git и CI/CD

```bash
# Ревью изменений перед коммитом
claude review --staged

# Генерация commit message
claude commit --auto-message

# Анализ PR
claude review-pr 123

# Проверка workflows
claude check .github/workflows/
```

## Типичные сценарии использования

### 1. Создание новой подсистемы

```bash
# Шаг 1: Планирование
$ claude ask "Мне нужна система аналитики (4.13). Как начать?"

Claude предложит:
1. Изучить существующие системы (4.1-4.12)
2. Определить интеграционные точки
3. Выбрать tech stack
4. Спланировать архитектуру

# Шаг 2: Создание документации
$ claude doc create --template Agent-Template.md \
    --output content/4.\ Системы/4.13.\ Система\ аналитики.md

# Шаг 3: Генерация кода агента
$ claude implement "Создай агента Analytics по шаблону из agents-core/docs/"

# Шаг 4: Ревью
$ claude review artifacts/claude/
```

### 2. Улучшение существующего кода

```bash
# Анализ качества кода
$ claude analyze agents-core/docs/agent.py --focus performance,security

# Рефакторинг с объяснениями
$ claude refactor agents-core/docs/agent.py --explain

# Добавление тестов
$ claude test generate agents-core/docs/agent.py --coverage 90
```

### 3. Отладка проблемы

```bash
# Описание проблемы
$ claude debug "ops/build_check_document.py падает с MemoryError на больших файлах"

Claude:
1. Проанализирует код
2. Найдет проблему (загрузка всего файла в память)
3. Предложит решение (streaming обработка)
4. Покажет код fix'а
5. Объяснит trade-offs
```

### 4. Планирование спринта

```bash
$ claude plan "Спланируй спринт на 2 недели для Backend команды"

Claude учтет:
- Текущие issues в GitHub
- Приоритеты из roadmap (0.4)
- Capacity команды
- Зависимости между задачами

Output: план спринта с оценками времени
```

### 5. Ревью документации

```bash
# Проверка всей документации
$ claude doc check content/ --full-report

Результат:
✅ 35 документов отлично (81%)
🟡 6 документов хорошо (14%)
🔴 2 требуют улучшений (5%)

Детали в artifacts/claude/reviews/
```

## Интеграция с Obsidian

Claude Code отлично работает с Obsidian vault:

### Shell Commands плагин

Добавьте команды в `.obsidian/plugins/shell-commands/`:

```json
{
  "commands": [
    {
      "name": "Claude: Analyze current file",
      "command": "claude analyze {{file_path}}",
      "hotkey": "Ctrl+Shift+A"
    },
    {
      "name": "Claude: Improve documentation",
      "command": "claude doc improve {{file_path}}",
      "hotkey": "Ctrl+Shift+D"
    },
    {
      "name": "Claude: Ask about selection",
      "command": "claude ask '{{selection}}'",
      "hotkey": "Ctrl+Shift+Q"
    }
  ]
}
```

### Workflow в Obsidian

1. Открываете документ в Obsidian
2. Выделяете текст
3. Жмете `Ctrl+Shift+Q` — Claude анализирует и отвечает
4. Результат сохраняется в `artifacts/claude/`

## Работа с AI-агентами

Claude Code может координировать других агентов:

```bash
# Запуск агента документации
$ claude agent run docs --task "Создать README для agents-core/analytics/"

# Проверка артефактов агента
$ claude agent review docs --artifact "artifacts/docs/drafts/analytics-readme.md"

# Координация нескольких агентов
$ claude orchestrate "Docs создает документацию, Architect проверяет архитектуру, Critique ревьюит"
```

## Best Practices

### ✅ Рекомендуется

1. **Давайте контекст**
   ```bash
   # Плохо
   claude "Добавь валидацию"

   # Хорошо
   claude "Добавь валидацию frontmatter метаданных в ops/classify_documents.py
           согласно схеме из content/0. Управление/0.7"
   ```

2. **Используйте итеративный подход**
   ```bash
   claude implement "Создай базовую версию агента"
   claude review artifacts/claude/agent-v1.py
   claude improve "Добавь обработку ошибок и логирование"
   ```

3. **Сохраняйте важные артефакты**
   ```bash
   # Claude сохраняет в artifacts/claude/
   # Важные результаты переносите в content/ или agents-core/
   cp artifacts/claude/analysis/architecture.md content/4.\ Системы/
   ```

4. **Проверяйте сгенерированный код**
   ```bash
   # Всегда ревьюите код перед коммитом
   claude review --staged
   git diff  # Проверьте изменения глазами
   ```

### ❌ Избегайте

1. **Не просите реализовать всё сразу**
   ```bash
   # Плохо
   claude "Реализуй всю систему аналитики"

   # Хорошо
   claude "Создай базовую структуру агента Analytics"
   claude "Добавь сбор метрик из Redis"
   claude "Добавь API endpoint для получения метрик"
   ```

2. **Не коммитьте без проверки**
   ```bash
   # Плохо
   claude implement "Fix bug" && git add . && git commit -m "fix"

   # Хорошо
   claude implement "Fix bug"
   claude review artifacts/claude/
   # Проверить код вручную
   git add specific-files
   git commit -m "fix: описание бага и решения"
   ```

3. **Не игнорируйте предупреждения**
   ```bash
   # Если Claude предупреждает о безопасности или performance
   # - изучите проблему
   # - попросите альтернативные решения
   # - не игнорируйте
   ```

## Примеры из реальной практики

### Пример 1: Создание системы классификации (0.7)

```bash
# Задача: разработать систему классификации документов

$ claude ask "Как классифицировать документы в content/?"

Claude предложил 6-осевую систему:
- Type (doc, data, code, etc.)
- Audience (manual, mixed, machine)
- Edit Mode (manual, mixed, machine)
- Layer (philosophy, methodology, ontology, etc.)
- Scope (global-core, local-edge)
- Security (public, internal, restricted)

$ claude implement "Реализуй классификацию в ops/classify_documents.py"

Результат: working script с AI-анализом документов
Время: ~30 минут (вместо нескольких часов вручную)
```

### Пример 2: Архитектура Memory Bank (4.12)

```bash
# Задача: спроектировать хранилище для 1M агентов

$ claude ask "Какую архитектуру выбрать для Memory Bank на 1M агентов?"

Claude проанализировал:
- Требования к масштабу (28k RPS, 15TB)
- Команду (10 человек)
- Бюджет

Предложил 4 варианта:
1. Cloud-native (Supabase + Pinecone) - $6-12k/мес
2. Hybrid Kubernetes (Proxmox+Ceph) - $15-25k/мес
3. Modern Data Platform (ClickHouse) - $20-35k/мес
4. Hybrid Cloud+Edge (Multi-region) - $25-40k/мес

$ claude doc create "Детальное описание архитектуры для 4.12"

Результат: production-ready документ с анализом
Утверждён командой: ✅ выбран Hybrid вариант
```

### Пример 3: Автоматизация CI/CD

```bash
# Задача: настроить автоматическую проверку документов

$ claude ask "Как автоматизировать проверку качества документации?"

Claude предложил:
1. GitHub Actions workflow
2. Проверки: frontmatter, markdown lint, links
3. Автосборка check document
4. Комментарии в PR

$ claude implement "Создай .github/workflows/content-ci.yml"

Результат: рабочий CI pipeline
Экономия: 5+ часов в неделю на ручных проверках
```

## Интеграция с GitHub

### Автоматический ревью PR

```bash
# В корне репозитория
$ claude setup github-integration

# Теперь Claude автоматически:
# - Ревьюит новые PR
# - Комментирует проблемы
# - Предлагает улучшения
# - Проверяет соответствие стандартам
```

### Работа с Issues

```bash
# Анализ issue
$ claude issue analyze 42

# Предложение решения
$ claude issue solve 42 --create-branch

# Claude создаст:
# - Ветку с fix'ом
# - Код решения
# - Тесты
# - Коммит
# - Готов к PR
```

## Конфигурация и расширения

### MCP Серверы

Claude Code поддерживает Model Context Protocol для расширения возможностей:

```bash
# Установка MCP сервера для работы с БД
claude mcp install @anthropic/postgres-mcp

# Теперь Claude может:
claude query "Сколько пользователей зарегистрировано за последний месяц?"
```

### Кастомные промпты

Создайте `.claude/prompts/` для специфичных задач:

```bash
# .claude/prompts/review-agent.md
Проверь агента на соответствие:
1. Agent-Template.md (структура)
2. Наличие всех обязательных секций
3. Frontmatter метаданные
4. Качество документации
5. Интеграция с экосистемой

# Использование
$ claude prompt review-agent artifacts/claude/new-agent.md
```

## Метрики и статистика

Claude Code собирает статистику использования:

```bash
# Показать статистику
$ claude stats

Сессий: 127
Задач выполнено: 543
Строк кода: 12,847
Документов создано: 23
Среднее время ответа: 3.2s
Успешность: 94%
```

## Устранение неполадок

### Проблема: Слишком большой контекст

```bash
# Ошибка: Context window exceeded (200k tokens)

# Решение 1: Уменьшить контекст
claude ask "..." --exclude "artifacts/**,*.zip"

# Решение 2: Разбить задачу
claude ask "Часть 1: анализ архитектуры"
claude ask "Часть 2: предложения по улучшению"
```

### Проблема: Медленные ответы

```bash
# Проверка
claude debug --check-api

# Возможные причины:
# - Большой контекст (уменьшите через --exclude)
# - Rate limits API (подождите или увеличьте лимиты)
# - Сложный запрос (разбейте на части)
```

### Проблема: Некорректные результаты

```bash
# Добавьте больше контекста
claude ask "..." --include "content/0. Управление/**"

# Попросите объяснить reasoning
claude ask "Объясни, почему ты выбрал такое решение"

# Итеративное улучшение
claude improve "Учти также требования из 4.12"
```

## Полезные ресурсы

### Документация
- [Claude Code Docs](https://docs.claude.com/en/docs/claude-code)
- [Anthropic API](https://docs.anthropic.com/)
- [MCP Protocol](https://modelcontextprotocol.io/)

### Внутренние документы проекта
- [README.md](./README.md) — общее описание проекта
- [CONTRIBUTING.md](./CONTRIBUTING.md) — правила участия
- [content/agents/Claude.md](content/agents/Claude.md) — паспорт агента Claude
- [content/0. Управление/](content/0.%20Управление/) — стандарты и процессы

### Сообщество
- GitHub Issues: для вопросов и багов
- Команда проекта: для консультаций по экосистеме

## FAQ

**Q: Сколько стоит использование Claude Code?**
A: API Anthropic платный. Примерная стоимость:
- Вход (prompt): $3 за 1M токенов
- Выход (completion): $15 за 1M токенов
- Средняя задача: $0.01-0.10

**Q: Можно ли использовать Claude Code offline?**
A: Нет, требуется подключение к Anthropic API.

**Q: Как защитить чувствительные данные?**
A: Используйте `.claude/settings.local.json` exclude для исключения файлов с секретами (.env, credentials, etc.)

**Q: Claude заменяет других агентов (Docs, Architect)?**
A: Нет. Claude — универсальный инструмент, специализированные агенты — для автоматизации специфичных задач.

**Q: Можно ли fine-tune Claude под проект?**
A: Пока нет. Но можно использовать промпты в `.claude/prompts/` для специализации.

## Обратная связь

Нашли проблему или есть предложение?
1. Создайте issue в GitHub
2. Опишите проблему с примером
3. Укажите версию Claude Code (`claude --version`)

---

**Версия:** 1.0
**Последнее обновление:** 2025-11-13
**Maintainer:** AI Platform Team
